# data-raw

This directory is for scripts that generate package data. The package currently
does not bundle large simulation outputs. Paper reproduction outputs are written
by `reproduce_paper()` to a user-selected directory.
